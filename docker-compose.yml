version: '3.8'

services:
  api:
    build: .
    ports:
      - "80:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL}
      # Add any other required env variables here
    restart: always
    # Removed the 'depends_on: db' since the DB is now managed by AWS RDS