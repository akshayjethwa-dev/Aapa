version: '3.8'

services:
  api:
    build: .
    ports:
      - "80:3000"
    # This automatically injects all variables from your .env file
    env_file:
      - .env
    restart: always
    # Database is managed by AWS RDS, so we do not declare a db service here